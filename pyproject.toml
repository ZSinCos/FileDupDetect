[project]
name = music-dup-detect
version = 1.0.0
description = 音乐重复文件检测工具
requires-python = ">=3.10"
dependencies = [
    "PySide6>=6.5.0",
    "mutagen>=1.47.0",
]

[project.optional-dependencies]
fingerprint = ["pyacoustid>=2.1.0"]